#include "Dog.h"
#include <iostream>
#include <string>
using namespace std;

int main(){
    /*
    Dog d1("Honor", 1, "Dr. Linstead"); //creating object on the stack, static -> allocated at compile
    Dog* d2 = new Dog("Lilah", 2, "Dr. Stevens"); //creating an object on the heap, dynamically allocates memory at runtime

    d1.speak();
    d2->speak();
    delete d2;
    */
    cout <<"hello world";

    return 0;
}